# ─────────────────────────────────────────
# OphyCare ResourceInn Auto Check-in/out
# ─────────────────────────────────────────
# Copy this to .env and fill in values
# NEVER commit .env to git

# ── OphyCare Credentials ──────────────────────────
RI_EMAIL=your.email@yourcompany.com
RI_PASSWORD=your_password_here

# ── Schedule (24-hour format, Asia/Karachi timezone) ──
# Times are in UTC format (PKT = UTC+5)
# Example: 11:00 UTC = 6:00 AM PKT (check-in)
#          20:00 UTC = 3:00 PM PKT (check-out)
CHECKIN_TIME=11:00
CHECKOUT_TIME=20:00
TZ=Asia/Karachi

# ── Office Location (for perimeter check) ───────────
OFFICE_LAT=24.8607
OFFICE_LNG=67.0011
OFFICE_RADIUS=200

# ── Features ─────────────────────────────
# Set to "true" to require location ping before check-in
# Set to "false" for pure time-based cron (no location needed)
REQUIRE_LOCATION=false

# How long a location ping is "valid" for (in ms). Default: 5 minutes
LOCATION_TIMEOUT_MS=300000

# ── Server ────────────────────────────────
PORT=3000
